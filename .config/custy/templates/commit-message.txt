feat(distribution): checkpoint private GitLab package delivery

Add the private GitLab PyPI distribution path for Path Header Scanner and
harden the release boundary before the v1.0.0-rc.1 promotion commit.

### 📦 Private package delivery

- Publish the `path-header-scanner` wheel and source distribution to the
  project-level GitLab PyPI registry after the protected-tag production image
  succeeds.
- Keep `path-header-scanner` as both the Python distribution and installed
  console-command name.
- Convert reviewed SemVer release tags to canonical PEP 440 package versions,
  including `v1.0.0-rc.1` → `1.0.0rc1` and `v1.0.0` → `1.0.0`.
- Use GitLab's short-lived `CI_JOB_TOKEN` for publication and document deploy
  tokens with `read_package_registry` for authorized consumers.
- Keep published versions immutable: duplicate uploads fail instead of
  overwriting or silently skipping an existing package.

### 🛡️ Validation and release safeguards

- Validate every pushed tag without granting every tag publication authority.
- Let unprotected tags complete package validation successfully while skipping
  production images, package publication, and provider-release creation.
- Require a protected, non-empty annotated release tag before any external
  production delivery job can run.
- Reject unsupported or ambiguous versions, lightweight tags, empty tag
  messages, and package metadata that does not match the selected Git tag.
- Build exactly one wheel and one source distribution, run `twine check`,
  verify filenames and embedded metadata, install the wheel in a clean virtual
  environment, and smoke-test `path-header-scanner --help` before publication.

### 🔄 Pipeline and documentation alignment

- Order the GitLab release path as test → package validation → production
  image → private package publication → provider release.
- Keep retained package artifacts available for inspecting validation-only tag
  pipelines.
- Update CI/CD guidance with the protected-tag contract, deploy-token install
  method, SemVer-to-PEP-440 mapping, forwarding warning, and retry behavior.
- Add structural regression coverage for validation-only unprotected tags and
  protected publication dependencies.

### 🧪 Validation

- Passed the full Path Header Scanner suite: 126 tests.
- Passed focused GitLab workflow structural checks: 7 tests.
- Passed Ruff, Black, GitLab YAML parsing, and repository diff checks.

This is development checkpoint 2 for the unpublished v1.0.0 release line. It
does not create a Git tag, provider release, container publication, or package
publication. The later RC.1 commit and annotated tag remain the release point.

Release target: v1.0.0-rc.1
Previous checkpoint: commit-message-v1.0.0-development-checkpoint.txt
Tag: none; this development checkpoint is intentionally untagged

🔖 **Tags**:
- Type: `#feature`, `#ci`, `#documentation`, `#testing`
- Delivery: `#gitlab-ci`, `#private-pypi`, `#package-registry`, `#protected-tags`
- Packaging: `#pep440`, `#semver`, `#wheel`, `#sdist`, `#twine`
- Safety: `#validation-only`, `#immutable-packages`, `#deploy-token`
- Stability: `#development-checkpoint`, `#untagged`
