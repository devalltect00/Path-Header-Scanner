# =========================================================
# 🐳 Development Docker Image
# =========================================================
#
# GitHub Equivalent
# -----------------
# .github/workflows/docker-dev.yml
#
# Purpose
# -------
# Build and publish development Docker images.
#
# Registry
# --------
# GitLab Container Registry:
#
#   registry.gitlab.com/<group>/<project>
#
# Development Tags
# ----------------
# dev
# <short-sha>
#
# Notes
# -----
# Development images may be overwritten frequently.
#
# =========================================================

docker:dev:
  stage: docker

  image: docker:27

  services:
    - docker:27-dind

  variables:
    DOCKER_TLS_CERTDIR: "/certs"

  before_script:
    - docker version

  script:
    - |
      echo "$CI_REGISTRY_PASSWORD" |
        docker login "$CI_REGISTRY" \
          -u "$CI_REGISTRY_USER" \
          --password-stdin

      docker build \
        --build-arg PHS_BUILD_VERSION="0.0.0.dev${CI_PIPELINE_IID}" \
        -f Dockerfile \
        --target development \
        -t "$CI_REGISTRY_IMAGE:dev" \
        -t "$CI_REGISTRY_IMAGE:${CI_COMMIT_SHORT_SHA}" \
        .

    - docker push "$CI_REGISTRY_IMAGE:dev"

    - docker push "$CI_REGISTRY_IMAGE:${CI_COMMIT_SHORT_SHA}"

  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'

    - if: '$CI_COMMIT_BRANCH == "dev"'
